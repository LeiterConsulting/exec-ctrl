# Persistent adoption snippet

Merge a customized version into existing target instructions only when persistent
adoption is in scope. Do not replace their contents or copy this file unedited.

```text
Use exec-ctrl for development tasks, scaled to the task and risk.
Method source: <repository URL at reviewed commit/ref>
Entry: <verified local START_HERE path or revision-specific URL>
Provenance: <commit, retrieval date and any local adaptations>
Read the entry contract and only relevant modules. Existing host, organization
and repository instructions retain their authority. Reuse our current task/PR
records. Do not install tools or change global settings just to load this method.
```

Use one canonical pointer with thin native adapters as needed. Check the target's
actual discovery mechanism, source access and future-session behavior. For offline
use, retain a reviewed local subset with its source revision and working relative
links. Updating that copy is a reviewed change, not an automatic upstream fetch.
