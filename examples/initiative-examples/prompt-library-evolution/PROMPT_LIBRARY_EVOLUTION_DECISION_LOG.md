> **v1 reference.** For new work use the [v2 activation contract](../../../START_HERE.md).
> Apply the workflow below only when explicitly maintaining a v1 adoption.

# Prompt Library Evolution Decision Log

| ID | Date | Decision | Rationale | Consequence |
| --- | --- | --- | --- | --- |
| `D-001` | `YYYY-MM-DD` | treat prompt-library coverage as initiative-mode work | the refinement improves one recurring activation surface without changing the core method | the prompt library can evolve in bounded slices |
| `D-002` | `YYYY-MM-DD` | update routing guidance together with the prompt pack | prompt additions are only useful if later sessions can discover them quickly | the example stays aligned with the generic method and workspace guidance |